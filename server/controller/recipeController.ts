import Recipe from '../models/recipe';
import { getIngredients } from '../scraper';

export async function scrapeRecipe(url: string) {
  // check if recipe exists
  // if exists return recipe
  // else scrape recipe, save to database and return?
}
